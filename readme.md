1. index.html

index.html is main page where we have only login function.
must have 2 input object, 1 submit button, two text with reset password and sign up function


1. index.html (Login Page)
	The entry point of the application, serving as the user authentication interface.
	Key Features:
	Form Inputs: 2 input fields (Email and Password).
	Actions: A Log In submit button to authenticate users.
	Navigation Links: Shortcuts to the Forgot Password page (Reset function) and the Sign Up page.
2. 